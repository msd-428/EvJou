# AGENTS.md — Codex の入口

> ⚠️ **このファイルはリポジトリルートの `AGENTS.md`（Codex 向け）です。**
> `.agents/AGENTS.md`（Antigravity 向け）とは**別のファイル**です。
> 名前が同じなので取り違えないでください。

このファイルは **Codex が自動で読む入口**です。**役割差分だけを書きます。**
ルール本体・ビルド手順・環境の罠・禁止事項は、すべて正典にあります。

## 最初に読む順番

1. **[.agents/PROJECT_RULES.md](.agents/PROJECT_RULES.md)** — 全エージェント共通の正典。
   役割憲章・引き継ぎ・ビルドとテスト・環境の罠・設計上の制約・事故台帳・文書マップ
2. **[.agents/STATE.md](.agents/STATE.md)** — 今どこにいるか。ブランチ・既知バグ・保留中の決定・次の一手
3. [docs/app-specification.md](docs/app-specification.md) — 仕様書
4. 変更対象に対応する正典文書（`PROJECT_RULES.md` §9 文書マップ）

## 役割

**実装の主担当、そして深掘りのコードレビューが主担当です。**
ローカル検証（`npm run build` / `npm run test:dataloss`）と差分の取りまとめも担当します。

チャットの文脈は他のエージェントと共有されません。
**セッションを始めたら `.agents/STATE.md` を読み、区切りがついたら更新してください。**
それが唯一の引き継ぎ経路です。更新の作法は `PROJECT_RULES.md` §2（**追記で更新する**）。

## 実機と外部環境に触らない

Codex のセッションでは **adb を実行しません。プロキシワーカーと Ollama を起動・停止しません。
Firestore を直接操作しません。**

実機や外部環境の状態が必要な調査に行き当たったら、自分で触らず、
**何が確認できれば判断できるか**を書いて止まってください。

理由は、実機の状態は「見た人がその場で記録したもの」以外は証拠にならないためです。
加えて、**実機にはユーザー本人の実データが入っており、クラウド同期はありません**。
消したら戻りません。

**★ 例外はこの入口では定義しません。** 例外の条件は
[.agents/PROJECT_RULES.md](.agents/PROJECT_RULES.md) §1 の Codex の節にあります。
そこに書かれた条件のもとでだけ許され、それ以外では上のとおり実行しません。
**チャットだけの許可では発効しません。**

## コードレビューを依頼されたとき

指摘には必ず**該当ファイルの行番号**と、**それが壊れる具体的な入力または状況**を添えてください。
「〜した方がよい」だけの指摘は、受け取る側が真偽を確認できないため採用されません。

確信が持てないものは「**確信度: 低**」と明記して出してください。
落とすより、確信度つきで出す方がよいです。

## 実装するとき

- **`src/features/useJournal.js`（ジャーナルの保存経路）に触れたら `npm run test:dataloss` を通し、
  実出力を報告に貼ってください。** これはこのアプリの生命線です
- 大きな一括書き換えは過去にパーサエラーを起こしています。**小さく変えて都度ビルド検証**します
- 完了条件の型は `PROJECT_RULES.md` §3、触ると壊れる設計上の制約は §7 にあります

## 指示が誤っていると分かったとき

黙って従うのも、黙って直すのも誤りです。**何が誤りで、なぜそう判断したか**を
（できればテストで実証して）示し、より良い案を提案してから進めてください。
