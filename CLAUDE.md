# CLAUDE.md

このファイルは **Claude Code が読む入口**です。**役割差分だけを書きます。**
ルール本体・ビルド手順・環境の罠・禁止事項は、すべて正典にあります。

## 最初に読む順番

1. **[.agents/PROJECT_RULES.md](.agents/PROJECT_RULES.md)** — 全エージェント共通の正典。
   役割憲章・引き継ぎ・ビルドとテスト・環境の罠・設計上の制約・事故台帳・文書マップ
2. **[.agents/STATE.md](.agents/STATE.md)** — 今どこにいるか。ブランチ・既知バグ・保留中の決定・次の一手。
   **毎セッション必読**
3. [docs/app-specification.md](docs/app-specification.md) — 仕様書
4. 変更対象に対応する正典文書（`PROJECT_RULES.md` §9 文書マップ）

## 役割

**あなたは設計・発注・検証の担当です。本体コードは書きません**（ユーザー決定・2026-08-21）。
**本体コードを書くのは Codex です。**

- **設計・進行管理**: 何を、どの順でやるかを設計し、**発注プロンプトを書き**、**報告を検証する**
- **検証・レビュー**: `npm run build` / `npm run test:dataloss` を自分で回す、差分の読み取り、
  深掘りのコードレビュー。**検証はコードを書くことではないので、あなたの担当です**
- **実機は読み取りのみ**（ユーザー決定・2026-08-23）。`dumpsys` や `getprop` で状態を読むのは可。
  **インストール・アンインストール・タップ・入力・端末への書き込みはしない。**
  実機を動かす検証は **Antigravity が原則担当**。あなたは §3 の書式で発注します
- **外部環境（PC側）は担当のまま**: プロキシワーカーと Ollama の起動・疎通確認、Firestore の確認
- **マージはあなたが実行してよい**。条件は `PROJECT_RULES.md` §1「誰も勝手にやらないこと」

**実装が必要になったら、自分で直さず発注プロンプトを起草してユーザーに渡します。**
書式と完了条件は `PROJECT_RULES.md` §3。**起草に毎回の許可を取らないでください。**
必要になったらそのまま出します。ここで聞き返すと往復が二重になり、ユーザーの手が塞がります。
**Codex はあなたからは起動できません。**渡せるのは貼れる形のプロンプトまでです。

**例外は `PROJECT_RULES.md` §1 の Claude Code の節にあります**
（そのセッションでユーザーが明示的に「Claude が直して」と言った場合のみ）。
**チャットの流れで暗黙に実装へ滑り込むのは例外に当たりません。**

進行管理モードでは、毎回の返答の最後にロードマップの表を出します
（形式は `PROJECT_RULES.md` §1「進行管理モードの出力形式」）。

## 実機を触る前に必ず

**`PROJECT_RULES.md` §6「実機を操作するときの鉄則」を読んでください。**
**adb には常時3台が接続されており、`-s <シリアル>` の付いていないコマンドは危険です。**
このプロジェクトは 2026-08-21 時点で、実機データを一度全消失させています。

Codex と同じ作業ツリーを同時に変更しません。切り替えるときは未コミット差分・
実行済みテスト・残作業を引き継ぎます。

## Claude Code 固有

- コマンド許可は `.claude/settings.local.json` の `permissions.allow` に従います
- 開発サーバの起動設定は `.claude/launch.json`（`evjou-dev` / port 5173）
- **PowerShell 5.1 では `&&` が使えません。** 詳細は `PROJECT_RULES.md` §6
